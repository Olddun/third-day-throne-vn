# 验收表

| 标准 | 落地证据 |
| --- | --- |
| 旁白不显示身份标识 | `renderNode()` 对 `旁白` 隐藏 `#speaker`；Playwright 检查开场旁白 `nameplateVisible=false`。 |
| 每个具名说话人有对应立绘 | `speakerActors` 覆盖塞德里克、安塔莉亚、大将军、大臣、老书记官、侍女、侍从；`test/smoke.mjs` 扫描所有具名 speaker。 |
| 具名角色有身份标识 | `renderCast()` 为每个可见立绘同步创建 `.portrait-label`；移动端验收确认标签不换行。 |
| 当前发言者正常显示 | `.portrait.speaking` 高亮，Playwright 检查大将军、塞德里克、安塔莉亚发言时对应立绘高亮。 |
| 在场但未发言者暗淡；不在场者隐藏 | `castForNode()` 按场景返回在场角色，未在 cast 中的 portrait/label 移除 `visible`；暗淡改用滤镜压暗，避免半透明重影。 |
| 开场不是只有猫 | `sceneCasts.throne` 包含塞德里克、大将军、大臣、老书记官、猫；Playwright 桌面/移动均检查开场五人立绘可见。 |
| 立绘有多表情差分 | 每个角色在 `assets.characters` 下至少 3 个表情资源，`test/smoke.mjs` 检查。 |
| 玩家身份清楚 | 标题页和选择面板明确玩家是在替塞德里克行动；选择面板显示 `塞德里克的选择。`。 |
| 选择简短但有行动感 | 玩家按钮只显示短行动，例如 `再看她一眼/逗她开口/轻碰耳尖/先查账册/公开证词/牵住她手`；hint 不再写入按钮。 |
| CG 不出现人物重影 | DOM/CSS 移除 `cg-live`、`cg-shot`、`cg-part` 与局部贴图 token；CG 动画只移动单张 `#event-cg` 加非图像光效；`soft_order.png` 已替换，塞德里克不再同侧两只手。 |
| VN 常用操作完整 | 对话框底部提供 `历史/自动/快进/分支/快存/快读/设置`；滚轮上滑打开历史，F5/F9 快速存读档。分支按钮打开按章节排列的路线回顾，只显示 10 个关键选择、选择语境和本轮已选项。 |
| 每个章节/小故事至少 3 个 CG | `storySections` 记录 6 段：序章 4 张、第一章 4 张、月圆/禁书库 4 张、第二章 3 张、第三章 3 张、终章 3 张；`test/smoke.mjs` 检查每段 >=3 且可达。 |
| 点击数与游戏时长 | 全分支 242 个可达节点；本次自玩通关路线 229 个剧情节点，228 次推进/选择。按 6.5 字/秒阅读速度估算约 24 分钟。 |
| 事件量足够 | 当前可达剧情事件约 253 个，包含 22 个选择条目、10 组选项面板、21 张可达 CG、6 个故事段落。 |
| 人物塑造不过早堆信息 | 开场只用身份角色承担功能，不展开背景；核心信息随行动展开：开笼、救猫、营地命名、旧王都哭泣、政变互救、公开审判。 |
| 符合救猫咪理论，有起伏 | 完整路线覆盖开场画面、主题陈述、催化事件、承诺同盟、B 故事信任线、游戏阶段、假胜利、坏人逼近、一无所有、灵魂黑夜、高潮破咒、终局画面。 |
| 去 AI 味，避免“不是而是”套路 | `rg` 与 `test/smoke.mjs` 检查 `不是/而是/不是...而是` 均无命中；实际渲染文本会按标点拆页，本轮完整自玩最大显示行 42 字。 |
| 自己验收桌面与移动端 | Edge/Playwright 跑桌面 1440x950 与移动 390x844：完整通关到 `大结局`，21 张 CG、10 组选项全出现，最大显示文本 42 字，断言 errors=[]。猫立绘截图验证不再被对话框遮住。 |
| 长篇重构目标已固化 | `web/story-plan.js` 记录 20 小时目标、18 个体验钩子、7 条人物弧光、12 个救猫咪节拍、14 个关键分歧、16 个结局、84 张规划 CG；`test/blueprint.mjs` 校验这些数量和字段。 |
| 不好玩禁止验收 | `funCheckpoints` 写入每场戏目标、选项代价、坏结局奖励、恋爱压力、反转频率等失败条件；其中明确写入“不能验收”，防止只按内容数量放行。 |
